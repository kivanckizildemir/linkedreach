[build]
  dockerfilePath = "backend/Dockerfile"
  buildCommand = ""

[deploy]
  startCommand = "node dist/combined.js"
  healthcheckPath = "/health"
  healthcheckTimeout = 120
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 5
